// import { fromJS } from 'immutable';
// import { makeSelectNodeDomain } from '../selectors';

// const selector = makeSelectNodeDomain();

describe('makeSelectNodeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
